# remove-lambda-eventbridge

*Note: this script is so violent that you must avoid executing this unless there are some special reasons.*

## Usage

```sh
AWS_PROFILE=<your-profile-for-awscli> AWS_REGION=<region> node main.js
```
